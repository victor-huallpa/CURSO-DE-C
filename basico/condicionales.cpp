#include <iostream>
using namespace std;

int main() {
    //variables
    bool pet = false;
    bool car = false;
    bool car2 = true;
    int val1 = 10;
    int val2 = 13;

    bool result;

    int numUser;

    //pedir datos
    cout << "ingrese numero; ";
    cin>>numUser;

    result = condition && condicion && condicion || concion !condicion ;

    if (result)
    {
       cout<<"aprovado";
    }else if (condition && condicion && condicion || concion !condicion)
    {
        /* code */
    }
    
     else{
        cout<<"desaprovado";
    }
    
    

    // codigo

    return 0;
}
